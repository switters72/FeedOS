# for main domain
RewriteCond %{HTTP_HOST} ^(www\.)?anonymousuniversity\.com$ [NC]
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP_HOST} ^www\. [NC]
RewriteRule ^ https://anonymousuniversity.com%{REQUEST_URI} [R=301,L,NE]